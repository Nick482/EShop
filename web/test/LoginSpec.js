/**
 * Created by Nick on 9/29/2015.
 */
define(function(require) {
    var Login = require('components/login/LoginView');

    describe("Login", function () {
        it("should return an object, i guess", function () {
            expect(false).toBe(false)
        })
    });
});